// Empty stub for the `server-only` package, used by Vitest via the alias
// in vitest.config.ts. The real module throws at import time to enforce
// server-only boundaries at build time; that protection is bundler-side
// and doesn't apply under tests.
export {};
