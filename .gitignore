# ── Credentials & secrets ───────────────────────────────────────────────
# Belt-and-braces: anything that *looks* like an env / secret never reaches git.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*service-role*
*service_role*
service-role-key*
.supabase/
supabase/.temp/

# Apply-all concatenations are forbidden — they drift silently from the
# real migrations folder. Run each migration file in order via psql or
# `supabase db push` instead. See INFRASTRUCTURE.md → Operational runbook.
supabase/_apply_all.sql
supabase/_apply*.sql

# ── Dependencies / build artefacts ──────────────────────────────────────
node_modules/
.next/
dist/
websocket-server/node_modules/
websocket-server/dist/
websocket-server/.env
build/
out/
coverage/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ── OS / editor ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp

# ── Tooling caches ──────────────────────────────────────────────────────
.npm-cache/
.cache/
